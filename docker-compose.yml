services:
  stream-watch:
    image: nginx:1.27-alpine
    container_name: stream-watch
    restart: unless-stopped
    ports:
      - "10080:10080"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./html:/usr/share/nginx/html:ro
